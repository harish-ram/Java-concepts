
package web;

/**
 * Legacy ad-hoc server removed — kept a tiny deprecated stub to avoid accidental compilation issues
 * while cleaning up references. Use Spring Boot (`main.SpringBootApp`) instead.
 */
@Deprecated
public final class Server {
    private Server() { /* no-op */ }
}
